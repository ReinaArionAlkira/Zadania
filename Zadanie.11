const readlineSync = require('readline-sync');

var hight= readlineSync.questionInt();

for (var i=1; i<=hight; i++) {
  var star = "*";
  var blindLine=" ";
  for (var j=1; j<i*2-1; j++) star += "*" ;
  for (var k=hight; k > i; k--) blindLine += " ";
  console.log(blindLine + star)
}
