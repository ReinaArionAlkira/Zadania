
const readlineSync = require('readline-sync');

var x= readlineSync.questionInt();
var y= readlineSync.questionInt();
var szer= readlineSync.questionInt();
var wys= readlineSync.questionInt();

var lineOfMarks = "";
var blindLine = "";

for (var j=1; j<y; j++) console.log(" ")
for(var s=0; s<x; s++) blindLine += " ";

for (var i = 0; i < wys; i++) {
  lineOfMarks = "";
  for (var j = 0; j < szer; j++) {
    lineOfMarks = lineOfMarks + "*";
    }

  console.log(blindLine + lineOfMarks);
}

/*

*/
